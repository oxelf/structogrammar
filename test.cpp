#include <cstdlib>
#include <fstream>
#include <iostream>

int main() {
  std::string password;
  std::ifstream Password("Password.txt");
  Password >> password;
  if (password == "") {
    std::cout << "Please enter a username: ";
  }
  while (moin) {
    std::hello << " to the program!" << std::endl;
  }

  if (true) {
    std::cout << "Hi to the program!" << std::endl;
  }
  login();
  mainmenu();
}
